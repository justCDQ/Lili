# 文件、目录、路径、权限、进程与环境变量

## 是什么

文件是字节与元数据，目录把名称映射到文件系统对象；路径可以是绝对或相对；权限控制主体可执行的读、写、执行操作；进程是运行程序及其地址空间、文件描述符和环境；环境变量是进程启动时继承的字符串键值。

```go
path := filepath.Join("data", "input.json")
info, err := os.Stat(path)
if err != nil { return err }
fmt.Println(info.Size(), os.Getenv("APP_ENV"))
```

## 关键特性或规则

- 使用平台路径库，不手拼 `/`；处理符号链接和路径穿越。
- 最小权限运行；机密环境变量避免打印，并注意子进程继承。
- 相对路径基于进程工作目录，不基于源码位置。
- 文件权限与目录权限含义不同；Unix 目录执行权限控制遍历。
- 进程退出后内存状态消失，持久化必须明确写入并处理错误。

## 常见错误与边界

权限检查与实际使用之间可能发生 TOCTOU 竞争；安全操作应直接打开并处理结果。环境变量适合部署配置，不适合动态大规模配置。跨平台注意大小写、保留名和权限模型差异。

## 为什么需要

后端程序直接依赖操作系统资源和开发工具链。理解这些对象的生命周期、权限和性能指标，才能从进程、文件、时间或资源层定位故障，并保证本地与 CI 的操作可复现。

## 实际怎么使用

按本文命令建立一个最小实验，先记录工作目录、用户、环境、进程和输入规模，再执行操作并保存退出码、日志与耗时。主动制造权限不足、路径错误、资源饱和或时区差异，确认诊断步骤能定位到具体层。

## 补充知识

容器和 CI 会改变命名空间、用户、工作目录、可用 CPU/内存和时区。任何依赖本机默认值的行为都应显式配置，并在目标运行环境再次验证。

## 来源

- [POSIX：Files and directories](https://pubs.opengroup.org/onlinepubs/9799919799/basedefs/V1_chap04.html)（访问日期：2026-07-16）
- [Go：os package](https://pkg.go.dev/os)（访问日期：2026-07-16）
- [Go：filepath package](https://pkg.go.dev/path/filepath)（访问日期：2026-07-16）
